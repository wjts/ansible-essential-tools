- name: prepare .ssh directory
  file:
    state: directory
    path: ~/.ssh
    mode: 0700
  tags: ssh_key
- name: generate SSH key
  openssh_keypair:
    path: "~/.ssh/{{ ansible_user }}"
    type: ed25519
    comment: "generated by ansible"
  tags: ssh_key
