- import_tasks: flathub.yaml
  tags: phpstorm
- name: Install phpstorm via flatpak
  community.general.flatpak:
    name: phpstorm
    state: present
    remote: flathub
  become: true
  tags: phpstorm
